@model IEnumerable<HW8.Model.ArtWork>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artist1.BirthCity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artist1.BirthCity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArtWorkTitle }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtWorkTitle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArtWorkTitle })
        </td>
    </tr>
}

</table>

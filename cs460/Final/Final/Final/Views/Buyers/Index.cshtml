@model IEnumerable<Final.Model.Buyer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@Html.DisplayFor(modelItem => item.BuyerName)</td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BuyerName }) |
            @Html.ActionLink("Details", "Details", new { id=item.BuyerName }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BuyerName })
        </td>
    </tr>
}

</table>

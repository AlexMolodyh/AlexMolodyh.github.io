@model IEnumerable<Final.Model.Seller>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@Html.DisplayFor(modelItem => item.SellerName)</td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SellerName }) |
            @Html.ActionLink("Details", "Details", new { id=item.SellerName }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SellerName })
        </td>
    </tr>
}

</table>
